[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.28"
optimizer = true
optimizer_runs = 200

[etherscan]
sepolia = { key = "${ETHERSCAN_API}" }

[rpc_endpoints]
sepolia = "${ETH_RPC_URL}"
